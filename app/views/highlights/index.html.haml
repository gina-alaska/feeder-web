%h1 Listing highlights
%table
  %thead
    %tr
      %th User
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @highlights.each do |highlight|
      %tr
        %td= highlight.user_id
        %td= highlight.description
        %td= link_to 'Show', highlight
        %td= link_to 'Edit', edit_highlight_path(highlight)
        %td= link_to 'Destroy', highlight, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Highlight', new_highlight_path
