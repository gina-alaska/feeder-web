= bootstrap_nested_form_for(@slideshow) do |f|
  = f.alert_message "Please fix the errors below."
  = f.text_field :title
  = f.check_box :highlights_only
  
  .form-group.pull-right
    = link_to slideshows_path, class: 'btn btn-default' do
      Cancel
    = f.primary
